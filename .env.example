DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dynasty?schema=public"

# Amazon Associates tracking ID (e.g. "dynastica-20"). Leave empty pre-approval —
# links still render but won't earn commission until set.
NEXT_PUBLIC_AMAZON_TAG=""
